from django.contrib import admin
from.models import contact_info,products,category

# Register your models here.
admin.site.register(contact_info)
admin.site.register(products)
admin.site.register(category)
